/*arr = [2, 7, 5];
arrCopy = [];

for (i = 0; i < arr.length; i++) {
  arrCopy[i] = arr[i];
}

arrCopy.push();
console.log(arr, arrCopy);
*/

arr = [6, 0, 33, 9, 50];
arrCopy = arr.map((x) => x);

console.log (arr, arrCopy)